# Free key from https://www.pexels.com/api — used at build time to fetch a themed photo
# per article (src/lib/pexels-client.ts). Optional: articles without it (or without their
# own `imageQuery` frontmatter field) fall back to the generated ArticleSeal instead.
PEXELS_API_KEY=
